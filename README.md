# ProductsRestAPI

This project is built using python, flask and mongodb which aims to perform the CRUD operations.

It includes Items API, Order API and User.

User has to login with userId and password to generate token.
Authorize with the generated token  and then perform crud operations for items and order.
